# Base URL of the backend API. Point this at whatever your teammates stand up.
VITE_API_BASE_URL=http://localhost:4000/api

# Set to "true" to use the built-in mock data layer instead of calling a real
# backend. Useful for building/testing the UI before the API exists.
VITE_USE_MOCKS=true
